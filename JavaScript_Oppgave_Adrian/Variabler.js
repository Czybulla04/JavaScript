// Variabler

// String

let navn = "Adrian "; // Definerer navn til verdi "Adrian"
let mellomnavn = "Czybulla "; // Definerer mellomnavn til verdi "Czybulla"
let etternavn = "Eskeland"; // Definerer etternavn til verdi "Eskeland"
let navnMellomnavnEtternavn = navn + mellomnavn + etternavn; // Legger alle verdiene sammen
console.log(navnMellomnavnEtternavn); // Printer ut resultatet

// Number

let a = 3.14; // Definerer a til verdi 3.14
let b = 100; // Definerer b til verdi 100
let c = -15; // Definerer c til verdi -15
let sum = a + b + c; // Legger alle verdiene sammen
console.log(sum); // Printer ut resultatet

// Boolean

let kaldt = true; // Definerer om variabel kaldt er true eller false
let vind = true; // Definerer om variabel vind er true eller false
if (kaldt && vind === true) { // Om variabel kaldt og vind begge er true
    console.log("Ta på jakke") // Print ut "Ta på jakke"
} else { // Hvis ikke
    console.log("Ta på genser") // Print ut "Ta på genser"
}